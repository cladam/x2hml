// Generated by hica (test mode)
module yml2hml_test

import yml2hml

extern hica-exit( code : int ) : a
  c inline "(exit(kk_integer_clamp32(#1, kk_context())), kk_box_null())"

fun hc-assert( b : bool ) : exn ()
  if !b then throw("assertion failed")

fun hc-show-maybe(m, show-a)
  match m
    Nothing -> "None"
    Just(x) -> "Some(" ++ show-a(x) ++ ")"

fun hc-show-result(r, show-a, show-e)
  match r
    Right(x) -> "Ok(" ++ show-a(x) ++ ")"
    Left(e)  -> "Err(" ++ show-e(e) ++ ")"

fun hc_abs(n : int)
  if (n < 0) then (0 - n) else n

fun hc_min(a : int, b : int)
  if (a < b) then a else b

fun hc_max(a : int, b : int)
  if (a > b) then a else b

fun hc_clamp(v : int, lo : int, hi : int)
  hc_min(hc_max(v, lo), hi)

fun hc_gcd(a : int, b : int)
  if (b == 0) then a else hc_gcd(b, (a % b))

fun hc_lcm(a : int, b : int)
  if ((a == 0) || (b == 0)) then 0 else (hc_abs((a * b)) / hc_gcd(a, b))

fun hc_pow(base : int, exp : int)
  if (exp <= 0) then 1 else (base * hc_pow(base, (exp - 1)))

fun hc_sign(n : int) : int
  if (n > 0) then 1 else if (n < 0) then (0 - 1) else 0

fun hc_range(lo : int, hi : int)
  if (lo >= hi) then [] else ([lo] ++ hc_range((lo + 1), hi))

fun hc_range_inc(lo : int, hi : int)
  if (lo > hi) then [] else ([lo] ++ hc_range_inc((lo + 1), hi))

fun hc_isqrt(n : int)
  if (n <= 0) then
    0
  else
    val x0 : int = n
    val x1 : int = ((x0 + 1) / 2)
    val result : int = hc_isqrt_loop(n, x0, x1)
    result

fun hc_isqrt_loop(n : int, x0 : int, x1 : int)
  if (x1 >= x0) then
    x0
  else
    val x2 : int = ((x1 + (n / x1)) / 2)
    hc_isqrt_loop(n, x1, x2)

fun hc_is_digit(c : char) : bool
  val n : int = c.int
  ((n >= 48) && (n <= 57))

fun hc_is_upper(c : char) : bool
  val n : int = c.int
  ((n >= 65) && (n <= 90))

fun hc_is_lower(c : char) : bool
  val n : int = c.int
  ((n >= 97) && (n <= 122))

fun hc_is_alpha(c : char) : bool
  (hc_is_upper(c) || hc_is_lower(c))

fun hc_is_alnum(c : char) : bool
  (hc_is_alpha(c) || hc_is_digit(c))

fun hc_is_space(c : char) : bool
  val n : int = c.int
  ((((n == 32) || (n == 9)) || (n == 10)) || (n == 13))

fun hc_is_punct(c : char) : bool
  val n : int = c.int
  (((((n >= 33) && (n <= 47)) || ((n >= 58) && (n <= 64))) || ((n >= 91) && (n <= 96))) || ((n >= 123) && (n <= 126)))

fun hc_all_digits(s : string) : bool
  if (s.count == 0) then False else all(s.string/list, hc_is_digit)

fun hc_all_alpha(s : string) : bool
  if (s.count == 0) then False else all(s.string/list, hc_is_alpha)

fun hc_all_upper(s : string) : bool
  if (s.count == 0) then False else all(s.string/list, hc_is_upper)

fun hc_all_lower(s : string) : bool
  if (s.count == 0) then False else all(s.string/list, hc_is_lower)

fun hc_all_alnum(s : string) : bool
  if (s.count == 0) then False else all(s.string/list, hc_is_alnum)

fun hc_glob_match_chars(pat : list<char>, input : list<char>)
  match pat
    Nil ->
      match input
        Nil ->
          True
        _ ->
          False
    Cons(p, prest) ->
      match p.string
        "?" ->
          match input
            Nil ->
              False
            Cons(c, irest) ->
              if (c.string == "/") then False else hc_glob_match_chars(prest, irest)
        "*" ->
          match prest
            Nil ->
              all(input, fn(c) (c.string != "/"))
            _ ->
              hc_glob_star(prest, input)
        _ ->
          match input
            Nil ->
              False
            Cons(c, irest) ->
              if (p == c) then hc_glob_match_chars(prest, irest) else False

fun hc_glob_star(prest : list<char>, input : list<char>)
  if hc_glob_match_chars(prest, input) then
    True
  else
    match input
      Nil ->
        False
      Cons(c, irest) ->
        if (c.string == "/") then False else hc_glob_star(prest, irest)

fun hc_glob_match(pattern : string, s : string)
  hc_glob_match_chars(pattern.string/list, s.string/list)

fun hc_glob_match_path(pattern : string, hc_path : string)
  val pat_parts : list<string> = (fn(hc__s: string, hc__sep: string) if hc__sep.is-empty then hc__s.list.map(fn(c) c.string) else hc__s.split(hc__sep))(pattern, "/")
  val path_parts : list<string> = (fn(hc__s: string, hc__sep: string) if hc__sep.is-empty then hc__s.list.map(fn(c) c.string) else hc__s.split(hc__sep))(hc_path, "/")
  hc_glob_match_segments(pat_parts, path_parts)

fun hc_glob_match_segments(pats : list<string>, paths : list<string>)
  match pats
    Nil ->
      match paths
        Nil ->
          True
        _ ->
          False
    Cons(p, prest) ->
      if (p == "**") then
        hc_glob_doublestar(prest, paths)
      else
        match paths
          Nil ->
            False
          Cons(s, srest) ->
            if hc_glob_match(p, s) then hc_glob_match_segments(prest, srest) else False

fun hc_glob_doublestar(prest : list<string>, paths : list<string>)
  if hc_glob_match_segments(prest, paths) then
    True
  else
    match paths
      Nil ->
        False
      Cons(_, srest) ->
        hc_glob_doublestar(prest, srest)

fun hc_is_empty(s : string)
  (s.count == 0)

fun hc_is_blank(s : string)
  hc_is_empty(s.trim)

fun hc_words(s : string)
  filter((fn(hc__s: string, hc__sep: string) if hc__sep.is-empty then hc__s.list.map(fn(c) c.string) else hc__s.split(hc__sep))(s, " "), fn(w) !(hc_is_empty(w)))

fun hc_lines(s : string)
  (fn(hc__s: string, hc__sep: string) if hc__sep.is-empty then hc__s.list.map(fn(c) c.string) else hc__s.split(hc__sep))(s, "\n")

fun hc_unwords(ws : list<string>)
  ws.join(" ")

fun hc_unlines(ls : list<string>)
  ls.join("\n")

fun hc_repeat_str(s : string, n : int)
  if (n <= 0) then "" else (s ++ hc_repeat_str(s, (n - 1)))

fun hc_pad_left(s : string, width : int, ch : string)
  (hc_repeat_str(ch, hc_max(0, (width - s.count))) ++ s)

fun hc_pad_right(s : string, width : int, ch : string)
  (s ++ hc_repeat_str(ch, hc_max(0, (width - s.count))))

fun hc_center(s : string, width : int, ch : string)
  val total : int = hc_max(0, (width - s.count))
  val left : int = (total / 2)
  val right : int = (total - left)
  ((hc_repeat_str(ch, left) ++ s) ++ hc_repeat_str(ch, right))

fun hc_removeprefix(s : string, pre : string) : string
  if s.starts-with(pre).is-just then s.list.drop(pre.count).string else s

fun hctest0()
  hc-assert((hc_count_indent("")) == (0))

fun hctest1()
  hc-assert((hc_count_indent("key: val")) == (0))

fun hctest2()
  hc-assert((hc_count_indent("  key: val")) == (2))

fun hctest3()
  hc-assert((hc_count_indent("    value")) == (4))

fun hctest4()
  hc-assert((hc_to_hml_key("my_key_name")) == ("my-key-name"))

fun hctest5()
  hc-assert((hc_to_hml_key("key")) == ("key"))

fun hctest6()
  hc-assert((hc_hml_value("hello")) == ("\"hello\""))

fun hctest7()
  hc-assert((hc_hml_value("42")) == ("42"))

fun hctest8()
  hc-assert((hc_hml_value("3.14")) == ("3.14"))

fun hctest9()
  hc-assert((hc_hml_value("true")) == ("true"))

fun hctest10()
  hc-assert((hc_hml_value("yes")) == ("true"))

fun hctest11()
  hc-assert((hc_hml_value("false")) == ("false"))

fun hctest12()
  hc-assert((hc_hml_value("off")) == ("false"))

fun hctest13()
  hc-assert((hc_hml_value("null")) == ("null"))

fun hctest14()
  hc-assert((hc_hml_value("~")) == ("null"))

fun hctest15()
  hc-assert((hc_hml_value("")) == ("\"\""))

fun hctest16()
  hc-assert((hc_hml_value("\"hello world\"")) == ("\"hello world\""))

fun hctest17()
  hc-assert((hc_hml_value("'hello'")) == ("\"hello\""))

fun hctest18()
  hc-assert((hc_hml_value("[a, b, c]")) == ("[\"a\", \"b\", \"c\"]"))

fun hctest19()
  hc-assert((hc_strip_inline_comment("value")) == ("value"))

fun hctest20()
  hc-assert((hc_strip_inline_comment("value # note")) == ("value"))

fun hctest21()
  hc-assert((hc_strip_inline_comment("\"val # not a comment\"")) == ("\"val # not a comment\""))

fun hctest22()
  hc-assert((hc_find_key_sep("key: value")) == (3))

fun hctest23()
  hc-assert((hc_find_key_sep("key:")) == (3))

fun hctest24()
  hc-assert((hc_find_key_sep("url: https://example.com")) == (3))

fun hctest25()
  hc-assert((hc_find_key_sep("just a value")) == ((0 - 1)))

fun hctest26()
  hc-assert((hc_convert_flow_seq("[]")) == ("[]"))

fun hctest27()
  hc-assert((hc_convert_flow_seq("[1, 2, 3]")) == ("[1, 2, 3]"))

fun hctest28()
  hc-assert((hc_convert_flow_seq("[a, b]")) == ("[\"a\", \"b\"]"))

fun hctest29()
  hc-assert((hc_convert_flow_map("{}", "elem")) == ("@elem"))

fun hctest30()
  hc-assert((hc_convert_flow_map("{name: foo}", "tag")) == ("@tag(name: \"foo\")"))

fun hctest31()
  hc-assert((hc_yaml_to_hml("key: value")) == ("key: \"value\""))

fun hctest32()
  hc-assert((hc_yaml_to_hml("count: 5")) == ("count: 5"))

fun hctest33()
  hc-assert((hc_yaml_to_hml("my_key: yes")) == ("my-key: true"))

fun hctest34()
  hc-assert((hc_yaml_to_hml("# a note")) == ("// a note"))

fun hctest35()
  hc-assert((hc_yaml_to_hml("items:\n  - a\n  - b")) == ("items: [\"a\", \"b\"]"))

fun hctest36()
  hc-assert((hc_yaml_to_hml("parent:\n  child: value")) == ("@parent {\n    child: \"value\"\n}"))

fun hctest37()
  hc-assert((hc_yaml_to_hml("users:\n  - name: alice\n    age: 30\n  - name: bob\n    age: 25")) == ("@users {\n    name: \"alice\"\n    age: 30\n}\n@users {\n    name: \"bob\"\n    age: 25\n}"))

fun hctest38()
  hc-assert((hc_yaml_to_hml("item: {name: foo, enabled: yes}")) == ("@item(name: \"foo\", enabled: true)"))

fun hctest39()
  hc-assert((hc_yaml_to_hml("desc: |\n  hello\n  world")) == ("desc: \"\"\"\nhello\nworld\n\"\"\""))

fun main()
  println("")
  println("running 40 test(s)...")
  println("")
  val r0 = match try({ hctest0() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "count_indent: empty string")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "count_indent: empty string")
      println("    " ++ e.message)
      False
  val r1 = match try({ hctest1() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "count_indent: no indent")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "count_indent: no indent")
      println("    " ++ e.message)
      False
  val r2 = match try({ hctest2() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "count_indent: two spaces")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "count_indent: two spaces")
      println("    " ++ e.message)
      False
  val r3 = match try({ hctest3() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "count_indent: four spaces")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "count_indent: four spaces")
      println("    " ++ e.message)
      False
  val r4 = match try({ hctest4() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "to_hml_key: underscores become dashes")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "to_hml_key: underscores become dashes")
      println("    " ++ e.message)
      False
  val r5 = match try({ hctest5() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "to_hml_key: no underscores unchanged")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "to_hml_key: no underscores unchanged")
      println("    " ++ e.message)
      False
  val r6 = match try({ hctest6() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: bare string gets quoted")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: bare string gets quoted")
      println("    " ++ e.message)
      False
  val r7 = match try({ hctest7() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: integer stays unquoted")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: integer stays unquoted")
      println("    " ++ e.message)
      False
  val r8 = match try({ hctest8() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: float stays unquoted")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: float stays unquoted")
      println("    " ++ e.message)
      False
  val r9 = match try({ hctest9() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: true bool")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: true bool")
      println("    " ++ e.message)
      False
  val r10 = match try({ hctest10() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: yes maps to true")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: yes maps to true")
      println("    " ++ e.message)
      False
  val r11 = match try({ hctest11() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: false bool")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: false bool")
      println("    " ++ e.message)
      False
  val r12 = match try({ hctest12() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: off maps to false")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: off maps to false")
      println("    " ++ e.message)
      False
  val r13 = match try({ hctest13() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: null")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: null")
      println("    " ++ e.message)
      False
  val r14 = match try({ hctest14() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: tilde is null")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: tilde is null")
      println("    " ++ e.message)
      False
  val r15 = match try({ hctest15() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: empty string")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: empty string")
      println("    " ++ e.message)
      False
  val r16 = match try({ hctest16() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: already double-quoted passthrough")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: already double-quoted passthrough")
      println("    " ++ e.message)
      False
  val r17 = match try({ hctest17() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: single-quoted becomes double-quoted")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: single-quoted becomes double-quoted")
      println("    " ++ e.message)
      False
  val r18 = match try({ hctest18() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "hml_value: flow sequence")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "hml_value: flow sequence")
      println("    " ++ e.message)
      False
  val r19 = match try({ hctest19() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "strip_inline_comment: no comment")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "strip_inline_comment: no comment")
      println("    " ++ e.message)
      False
  val r20 = match try({ hctest20() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "strip_inline_comment: strips trailing comment")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "strip_inline_comment: strips trailing comment")
      println("    " ++ e.message)
      False
  val r21 = match try({ hctest21() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "strip_inline_comment: quoted value preserves hash")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "strip_inline_comment: quoted value preserves hash")
      println("    " ++ e.message)
      False
  val r22 = match try({ hctest22() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "find_key_sep: finds colon-space")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "find_key_sep: finds colon-space")
      println("    " ++ e.message)
      False
  val r23 = match try({ hctest23() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "find_key_sep: key-only colon at end")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "find_key_sep: key-only colon at end")
      println("    " ++ e.message)
      False
  val r24 = match try({ hctest24() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "find_key_sep: url colon ignored")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "find_key_sep: url colon ignored")
      println("    " ++ e.message)
      False
  val r25 = match try({ hctest25() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "find_key_sep: no separator")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "find_key_sep: no separator")
      println("    " ++ e.message)
      False
  val r26 = match try({ hctest26() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "convert_flow_seq: empty sequence")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "convert_flow_seq: empty sequence")
      println("    " ++ e.message)
      False
  val r27 = match try({ hctest27() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "convert_flow_seq: int items")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "convert_flow_seq: int items")
      println("    " ++ e.message)
      False
  val r28 = match try({ hctest28() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "convert_flow_seq: string items")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "convert_flow_seq: string items")
      println("    " ++ e.message)
      False
  val r29 = match try({ hctest29() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "convert_flow_map: empty map")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "convert_flow_map: empty map")
      println("    " ++ e.message)
      False
  val r30 = match try({ hctest30() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "convert_flow_map: single pair")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "convert_flow_map: single pair")
      println("    " ++ e.message)
      False
  val r31 = match try({ hctest31() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: scalar key")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: scalar key")
      println("    " ++ e.message)
      False
  val r32 = match try({ hctest32() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: integer value")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: integer value")
      println("    " ++ e.message)
      False
  val r33 = match try({ hctest33() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: underscore key converted")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: underscore key converted")
      println("    " ++ e.message)
      False
  val r34 = match try({ hctest34() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: comment converted")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: comment converted")
      println("    " ++ e.message)
      False
  val r35 = match try({ hctest35() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: scalar list")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: scalar list")
      println("    " ++ e.message)
      False
  val r36 = match try({ hctest36() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: nested object becomes element block")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: nested object becomes element block")
      println("    " ++ e.message)
      False
  val r37 = match try({ hctest37() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: list of objects becomes repeated elements")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: list of objects becomes repeated elements")
      println("    " ++ e.message)
      False
  val r38 = match try({ hctest38() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: flow map becomes inline element")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: flow map becomes inline element")
      println("    " ++ e.message)
      False
  val r39 = match try({ hctest39() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_to_hml: block scalar becomes triple quoted string")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_to_hml: block scalar becomes triple quoted string")
      println("    " ++ e.message)
      False
  println("")
  val passed = [r0, r1, r2, r3, r4, r5, r6, r7, r8, r9, r10, r11, r12, r13, r14, r15, r16, r17, r18, r19, r20, r21, r22, r23, r24, r25, r26, r27, r28, r29, r30, r31, r32, r33, r34, r35, r36, r37, r38, r39].filter(fn(x) x).length
  val failed = 40 - passed
  if failed == 0 then
    println("\x1b[32m" ++ passed.show ++ " test(s) passed\x1b[0m")
  else
    println("\x1b[31m" ++ failed.show ++ " test(s) failed\x1b[0m, " ++ passed.show ++ " passed")
    hica-exit(1)

